import React from 'react';
import './App.css';
import View from './components/view';

/**
 * @returns {React.FunctionComponent} The application's root component.
 */
const App: React.FunctionComponent = () => {
  return (
    <View />
  );
};

export default App;
